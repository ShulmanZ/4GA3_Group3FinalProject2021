---
title: "Group 3's 4GA3 Final Project"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
library(tidyverse)
library(ggmap)
library(geosphere)
library(sf)
library(plotly)
library(spdep)
library(geog4ga3)
```

```{r}
#This is the code we will be using throughout this entire project
#Use this

#Group Names:
#Zack
#Devon
#Ignacio
#Owen
#Lysha


#Owen: not sure how to import a dataset so everyone can see it, but i have the the xls file for the neighbourhoods

library(readxl)
Neighbourhoods_fields <- read_excel("~/McMaster/ENVSOCTY 4GA3/FinalProject/Neighbourhoods_fields.xls")
summary(Neighbourhoods_fields)


```

